# === Data — NEVER commit licensed / market data ===
*.xlsx
*.xls
*.csv
*.tsv
data/
data_raw/
data_tidy/
real_data_for_mock/

# === Engine outputs / runtime artifacts ===
mg_exports/
falsify_diagnostic/
*.log
*.zip
mg_perf_log*

# === Secrets / local environment ===
.Renviron
.env
*.key
*.pem
secrets*

# === R / editor / OS ===
.Rproj.user/
.Rhistory
.RData
.DS_Store
*.swp

# === LaTeX build artifacts (keep the .pdf, drop the rest) ===
*.aux
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz
